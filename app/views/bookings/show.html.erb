<%= link_to bookings_path, style:"font-size: 30px; color:black" do %>
  <i class="fa-solid fa-arrow-left mt-3"></i>
<% end %>

<div class="row text-white rounded-3 bg-dark">
  <div class="col-sm-6 px-0">
    <div class="dog-card border-end">
    <img src="<%=@booking.dog.photos%>" class="card-img-left img-fluid" alt="Dog photo" style="object-fit: cover; width: 100%; height: 70vh;">
  </div>
  </div>
  <div class="col-sm-6">
    <div class="booking-card px-3" style="position:relative; top: 50%; transform: translateY(-50%);">
    <h1 class="py-4">You are fostering <%= @booking.dog.name %> for <%= @days %> days!</h1>
    <h4><%= @booking.dog.name %> has a <%= @booking.dog.personality %> personality 😎</h4>
    <h4><%= @booking.dog.name %> is of <%= @booking.dog.breed %> breed 🐶</h4>
    <h4><%= @booking.dog.name %> is <%= @age %> years old 🎂</h4>
    <h4><%= @booking.dog.name %> will be with you from:</h4>
    <h4 class="text-center"><strong><%= @booking.start_date.strftime("%A, %B %d, %Y") %> to <%= @booking.end_date.strftime("%A, %B %d, %Y") %></strong></h4>
    <div class="show-btns d-flex justify-content-center my-5">
      <%= link_to "Edit booking", edit_booking_path, class: "btn btn-primary text-white me-5"%>
      <%= link_to "Cancel booking", booking_path, class: "btn btn-danger text-white", data: {turbo_method: :delete, turbo_confirm: "Confirm booking cancellation?"}%>
    </div>
  </div>
  </div>
</div>
