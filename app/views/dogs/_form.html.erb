<%= form_with method: :get do |form| %>
    <% Dog.get_breeds.each do |breed| %>
        <%= form.check_box breed, data: {action: 'click->toggle-filter#checkboxSelect',
         toggle_filter_target: "checkboxElement",
         breed: breed} %>
        <%= form.label breed %>
    <% end %>
<% end %>
